# Required by Compose interpolation, even when infrastructure profile is off.
# Replace before running; use a randomly generated password, never this example.
POSTGRES_PASSWORD=replace-with-a-long-random-password
CLIP_DOMAIN=clip.example.com
